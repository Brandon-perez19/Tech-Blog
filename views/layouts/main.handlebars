<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>

<body>
    <div>
        <header class="row bg-light">
            <h1 class="col-xs-2 col-sm-4 h1 mt-2">
                Tech Blog
            </h1>
            <nav class="col">
                <ul class="row list-unstyled text-center mt-3">
                    <li class="col">
                        <a href="/" class="text-decoration-none text-muted">
                            Home
                        </a>
                    </li>
                    <li class="col">
                        <a href="/dashboard" class="text-decoration-none text-muted">
                            Dashboard
                        </a>
                    </li>
                    {{#if loggedIn}}
                    <li class="col">
                        <button id="logout"> Logout</button>
                    </li>
                    {{else}}
                    <li class="col">
                        <a href="/login" class="text-decoration-none text-muted">
                            Login
                        </a>
                    </li>
                    {{/if}}
                </ul>
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>
        <footer class="bg-light py-2">
            <h5 class="h5 text-center text-muted"> Thank you for Visiting </h5>
        </footer>
    </div>
    {{#if loggedIn}}
    <script src='/javascript/logout.js'></script>
    {{/if}}
</body>

</html>