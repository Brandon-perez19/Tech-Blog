<div class="my-2 mx-3">
    {{#each this}}
    <section class="card">
        <div class="card-header">
            Comment by: {{user.username}} on {{created_at}}
        </div>
        <div class="card-body">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>