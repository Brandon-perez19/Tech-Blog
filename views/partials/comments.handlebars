<div class="my-2 mx-3 comments">
    {{#each this}}
    <section class="card">
        <div class="card-header">
            Comment by: {{user.username}} on {{format_Date created_at}}
        </div>
        <div class="card-body">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>